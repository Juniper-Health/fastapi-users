[tool.poetry]
name = "fastapi-users"
version = "12.1.13"
description = "Ready-to-use and customizable users management for FastAPI"
authors = ["François Voron <fvoron@gmail.com>"]
readme = "README.md"
license = "MIT"
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Development Status :: 5 - Production/Stable",
    "Framework :: FastAPI",
    "Framework :: AsyncIO",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Internet :: WWW/HTTP :: Session",
]

[tool.poetry.dependencies]
python = ">=3.8"
fastapi = ">=0.65.2"
passlib = {extras = ["bcrypt"], version = "==1.7.4"}
email-validator = ">=1.1.0,<2.2"
pyjwt = {extras = ["crypto"], version = "==2.8.0"}
python-multipart = "==0.0.7"
makefun = ">=1.11.2,<2.0.0"
fastapi-users-db-sqlalchemy = {version = ">=6.0.0", optional = true}
fastapi-users-db-beanie = {version = ">=3.0.0", optional = true}
httpx-oauth = {version = ">=0.13", optional = true}
redis = {version = ">=4.3.3,<6.0.0", optional = true}

[tool.poetry.extras]
sqlalchemy = ["fastapi-users-db-sqlalchemy"]
beanie = ["fastapi-users-db-beanie"]
oauth = ["httpx-oauth"]
redis = ["redis"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
